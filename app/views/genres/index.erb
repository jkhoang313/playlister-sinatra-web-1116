<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Genres</title>
  </head>
  <body>
    <% @genres.each do |genre| %>
      <h2><%= genre.name %></h2>
      <a href="/genres/<%= genre.slug%>"/>
    <% end %>
  </body>
</html>
