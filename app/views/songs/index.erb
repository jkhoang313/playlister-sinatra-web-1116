<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Songs</title>
  </head>
  <body>
    <% @songs.each do |song| %>
      <h2><%= song.name %></h2>
      <a href="/songs/<%= song.slug%>"/>
    <% end %>
  </body>
</html>
