<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Artists</title>
  </head>
  <body>
    <% @artists.each do |artist| %>
      <h2><%= artist.name %></h2>
      <a href="/artists/<%= artist.slug%>"/>
    <% end %>
  </body>
</html>
